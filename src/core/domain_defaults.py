"""Stable defaults required by core domain models.

These values intentionally have no dependency on runtime configuration. The
operational configuration may override them at the application boundary.
"""

DEFAULT_DURATION_SEC = 900
DEFAULT_MIN_PAYOUT = 80
