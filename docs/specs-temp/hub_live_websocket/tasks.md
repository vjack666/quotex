# Tasks — hub_live_websocket

> Feature id=12. Marcar `[x]` al completar. Cada task referencia requirements.

---\n\n## Dependencias\n\n- [x] T1 — Añadir `fastapi>=0.100.0` y `uvicorn>=0.25.0` a `requirements.txt`. Cubre: R10.\n\n## Estructura del módulo hub/\n\n- [x] T2 — Crear `hub/server.py` (servidor FastAPI + WebSocket). Cubre: R1, R2, R6, R8, R11, R12, R13, R14, R15.\n- [x] T3 — Crear `hub/events.py` (sistema de eventos asíncronos: publicación, suscripción, cola). Cubre: R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14, R15.\n- [x] T4 — Crear `hub/__init__.py` (paquete vacío o con metadatos). Cubre: R1–R15 (estructura básica).\n\n## Comunicación bot → dashboard\n\n- [x] T5 — En `main.py`, importar y levantar el servidor de dashboard como tarea `asyncio` al iniciar el bot. Cubre: R2, R11.\n- [x] T6 — En `hub/events.py`, definir función `publish_event(event)` que el bot puede llamar para enviar señales, órdenes, capital, salud, etc. Cubre: R3, R4, R5, R6, R7, R8, R9.\n- [x] T7 — En `hub/server.py`, definir un `asyncio.Queue` o mecanismo similar para recibir eventos de `hub/events.py` y transmitirlos por WebSocket a clientes conectados. Cubre: R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14, R15.\n\n## Frontend (interfaz web embebida)\n\n- [x] T8 — En `hub/server.py`, servir una página HTML simple en la ruta raíz (`/`) que se conecta al WebSocket en `ws://localhost:8080/ws`. Cubre: R1, R2, R7, R8, R9, R10, R11, R12, R13, R14, R15.\n- [x] T9 — La página HTML debe mostrar en tiempo real:\n    - Señales recientes (símbolo, dirección, estrategia, score, payout, timestamp, razón de rechazo)\n    - Órdenes activas y recientes (símbolo, dirección, stake, resultado, hora de entrada/salida esperada)\n    - Capital actual, operaciones realizadas, ITM actuales, progreso hacia meta Massaniello (3/5)\n    - Salud del sistema: estado de conexión al broker, latencia promedio, ciclos completados, errores recientes\n    - Indicador de modo demo activado/desactivado\n    - Botón para pausar/reanudar la visualización de eventos (los eventos se bufferizan o se ignoran temporalmente)\n  Cubre: R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14, R15.\n- [x] T10 — Usar JavaScript vanilla (sin frameworks como React o Vue) para mantener el frontend ligero y compatible. Cubre: R10, R11.\n- [x] T11 — Asegurar que la interfaz sea responsiva y legible en móviles y escritorio. Cubre: R10, R11.\n\n## Modo de demostración\n\n- [x] T12 — En `hub/server.py`, leer variable de entorno `HUB_DEMO_MODE` y activar modo demo si es `true`. Cubre: R9.\n- [x] T13 — En modo demo, generar eventos simulados cada pocos segundos:\n    - Señales aleatorias (símbolos OTC, direcciones, scores entre 60-90)\n    - Órdenes simuladas con resultados aleatorios\n    - Capital que aumenta o disminuye según resultados simulados\n    - Salud del sistema: conexión simulada como \"OK\", latencia baja, ciclos incrementando\n  Cubre: R9.\n- [x] T14 — En modo demo, el estado inicial por WebSocket debe indicar `demo_mode: true`. Cubre: R7, R9.\n\n## Manejo de errores y resiliencia\n\n- [x] T15 — Si el servidor de dashboard falla al iniciar, registrar el error pero permitir que el bot continúe (no es crítico para el trading). Cubre: R11.\n- [x] T16 — Manejar conexiones WebSocket con timeouts y reconexión automática desde el frontend (JavaScript). Cubre: R11, R12.\n- [x] T17 — Opcional: bufferizar eventos brevemente en el backend si no hay clientes conectados (para no perder datos al reconectar). Cubre: R11, R12.\n- [x] T18 — Registrar logs de actividad del hub en el sistema de logging existente (`loguru`). Cubre: R11.\n\n## Salud y endpoint HTTP\n\n- [x] T19 — En `hub/server.py`, definir un endpoint HTTP `/health` que devuelva `{"status": "ok"}` si el servidor está activo. Cubre: R14.\n- [x] T20 — Asegurar que el endpoint `/health` sea accesible sin autenticación (para monitoreo sencillo). Cubre: R14.\n\n## Variables de entorno y configuración\n\n- [x] T21 — El servidor debe escuchar en `localhost` (127.0.0.1) por defecto, no en todas las interfaces. Cubre: R1.\n- [x] T22 — Permitir cambiar el host mediante variable de entorno `HUB_HOST=0.0.0.0` (opcional, no recomendado en producción sin autenticación). Cubre: R1.\n- [x] T23 — El puerto debe ser configurable mediante variable de entorno `HUB_PORT` (por defecto 8080). Cubre: R1.\n\n## Pruebas\n\n- [x] T24 — Crear `tests/test_hub_server.py` con tests unitarios de:\n    - Inicio del servidor\n    - Funcionamiento del endpoint `/health`\n    - Envío y recepción de mensajes WebSocket\n    - Modo de demostración\n    - No bloqueo del ciclo principal (simulado)\n  Cubre: R1–R15.\n- [x] T25 — Crear `tests/test_hub_events.py` con tests unitarios de:\n    - Publicación de eventos\n    - Suscripción y recepción de eventos\n    - Tipos de eventos definidos correctamente\n  Cubre: R3, R4, R5, R6, R7, R8, R9.\n\n## Cierre y verificación\n\n- [x] T26 — Ejecutar `python -m pytest tests/ -v` y verificar que todos los tests pasen (incluyendo los nuevos). Cubre: R1–R15.\n- [x] T27 — Ejecutar `.\\init.ps1` y verificar que termine con código de salida 0. Cubre: R12.\n- [x] T28 — Verificar manualmente:\n    - El servidor se inicia en `http://localhost:8080`\n    - Se puede acceder desde el navegador\n    - Se ven datos iniciales (o modo demo)\n    - Al simular o generar eventos del bot, se ven en tiempo real\n    - Se puede pausar/reanudar la visualización\n    - No se observa ralentización del bot principal\n    - Se puede cerrar el navegador sin detener el bot\n  Cubre: R1–R15.\n\n## Documentación y trazabilidad\n\n- [x] T29 — Crear `progress/impl_hub_live_websocket.md` y documentar el mapa completo `R<n> → nombre_de_test` para cada requirement. Cubre: R15.\n- [x] T30 — Actualizar `logs/CHANGELOG.md` con un resumen de los cambios realizados. Cubre: R1–R15.\n- [x] T31 — Actualizar `feature_list.json`: cambiar el status de `hub_live_websocket` de `pending` a `done`. Cubre: R1–R15.\n\n## Verificación del reviewer\n\n- [x] T32 — El reviewer verificará que:\n    - Las dependencias están en `requirements.txt`\n    - El servidor se inicia correctamente\n    - Los eventos del bot se transmiten por WebSocket\n    - El modo de demostración funciona\n    - El endpoint `/health` responde `{"status": "ok"}`\n    - No hay bloqueo del ciclo principal de trading\n    - Los tests pasan\n    - La trazabilidad R→test está documentada y verificada\n    - `.\\init.ps1` termina en verde\n  Cubre: R1–R15.\n\n---\n\n> Nota: Todas las tareas están diseñadas para ser verificables. El implementer debe marcar `[x]` al completar cada task. El reviewer verificará que todas las tareas estén completas antes de aprobar la feature.